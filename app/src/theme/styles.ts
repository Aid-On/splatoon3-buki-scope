// グローバルスタイル設定
// デフォルトのバックグラウンドカラーや文字色を設定します。

import type { StyleProps } from "@chakra-ui/styled-system";

const body: StyleProps = {
  // bg: 'teal.500',
  // color: 'white',
};

export const styles = {
  global: {
    body,
  },
};
